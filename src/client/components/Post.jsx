import React, { useState } from 'react';
import './post.scss';

// Props: data from row of database (or, when mocked, item in array)
// Render: data from row of database (or, when mocked, item in array)
// State: set state for new posts?